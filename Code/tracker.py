import cv2
import os
import labeler

directory = 'D:\ProgrammeerOmgeving\Projects\Tracking\Data'

training_data = labeler.data_labels
testing_data = []

for images2 in training_data:


#def check_difference(img, roi):
    #if(pixel == ROI):
        #make red
    #roi_gray = cv2.cvtColor(images2, cv2.COLOR_RGB2BGRAY)
    #roi_gray = cv2.GaussianBlur (roi_gray, (21, 21), 0)
    #cv2.absdiff(img, roi_value)
    #cv2.treshold() image moet grayscale zijn
